[
    {
        "guid": "f1a7b3c2-9d4e-4a86-bf21-6c0e5d83a47b",
        "name": "Aurelia Companion",
        "description": "Adds three independent APIs: server-side filtering by media-stream characteristics that Jellyfin's core API can't express (video dynamic range, codecs, audio channel count, and spatial audio), plus opt-in global recent-podcast discovery via Podcast Index using administrator-provided credentials, plus opt-in passwordless sign-in by emailed magic link over administrator-configured SMTP. The filtering API exposes a paginated, BaseItemDto-compatible endpoint and a capability endpoint for client feature-detection.\n",
        "overview": "Media-stream filtering, opt-in podcast discovery, and email sign-in.",
        "owner": "3dot3.com",
        "category": "General",
        "versions": [
            {
                "version": "1.6.0.0",
                "changelog": "- Initial release. - Resolution (isHd/is4K) passthrough for client filtering. - Fix: filtering by a library returned no items. The library (parentId) is now\n  resolved into its contents so video range / codec / audio filters work when\n  scoped to a specific library.\n- Renamed the plugin to \"Aurelia Companion\" (display name only; API route,\n  plugin id, and behavior unchanged).\n- Capabilities are now scoped to the requested library (parentId): the available\n  values offered for a library are drawn only from items in that library, so any\n  single facet selection is guaranteed to match at least one item there. Omitting\n  parentId still returns the global value set.\n- TV libraries now filter correctly by stream facets. Stream attributes live on\n  episodes, but TV libraries are browsed at the series level; the filter now\n  matches episodes and returns the shows that have at least one matching episode.\n  Also forces a recursive query so a library id resolves to its contents.\n- Added opt-in recent podcast discovery through Podcast Index. - Podcast discovery now logs why it returned nothing. The endpoint still answers 404 for\n  every failure, but the server log distinguishes disabled credentials, a Podcast Index\n  rejection, and feeds dropped during validation \u2014 including a per-rule tally of what was\n  dropped.\n- Removes the YouTube trailer resolver. Clients now hand YouTube trailers to the YouTube\n  app instead, so the server no longer downloads, verifies, updates, or executes yt-dlp or a\n  JavaScript runtime.\n- Removes the yt-dlp and JavaScript-runtime downloads earlier versions installed. Those\n  binaries are unreachable now the resolver is gone, so the plugin reclaims the directory it\n  created rather than leaving an administrator to find it. Only its own install root is\n  touched, and a failure to delete never stops the plugin starting.\n- Fix: Podcast Discovery always returned 404 when enabled. Podcast Index sends the envelope's\n  `status` as a JSON boolean, but it was bound to a string, so the whole recent-feeds payload\n  failed to deserialize. Both the boolean and the documented string form are now accepted, and\n  an unusable payload is logged with the value exactly as it arrived.\n- Added opt-in email sign-in. A client posts an email address, the server mails a single-use\n  link, and confirming that link signs the waiting device in \u2014 no password, and no typing on a\n  TV remote. The email can be opened on any device, so it works for tvOS; when it is opened on\n  the signing-in device itself, the confirmation page also offers a deep link straight back into\n  the app. Unrecognised addresses can optionally register, in which case the new account is\n  created disabled and copies an administrator-chosen template user's permissions, so nothing is\n  accessible until an administrator enables it. SMTP host, port, security, credentials and\n  from-address are configured in the plugin settings, along with link lifetime and rate limits;\n  a \"send test email\" button reports the mail server's own error when submission fails. The\n  endpoint reports an explicit error rather than a silent success when SMTP is not configured.\n",
                "targetAbi": "10.11.0.0",
                "sourceUrl": "https://github.com/drewtenn/Aurelia-Jellyfin-plugin-dist/releases/download/v1.6.0.0/aurelia-companion_1.6.0.0.zip",
                "checksum": "543fe3bc5dcdb50446d50f58431f39ee",
                "timestamp": "2026-08-10T02:20:51Z"
            },
            {
                "version": "1.5.2.0",
                "changelog": "- Initial release. - Resolution (isHd/is4K) passthrough for client filtering. - Fix: filtering by a library returned no items. The library (parentId) is now\n  resolved into its contents so video range / codec / audio filters work when\n  scoped to a specific library.\n- Renamed the plugin to \"Aurelia Companion\" (display name only; API route,\n  plugin id, and behavior unchanged).\n- Capabilities are now scoped to the requested library (parentId): the available\n  values offered for a library are drawn only from items in that library, so any\n  single facet selection is guaranteed to match at least one item there. Omitting\n  parentId still returns the global value set.\n- TV libraries now filter correctly by stream facets. Stream attributes live on\n  episodes, but TV libraries are browsed at the series level; the filter now\n  matches episodes and returns the shows that have at least one matching episode.\n  Also forces a recursive query so a library id resolves to its contents.\n- Added opt-in recent podcast discovery through Podcast Index. - Podcast discovery now logs why it returned nothing. The endpoint still answers 404 for\n  every failure, but the server log distinguishes disabled credentials, a Podcast Index\n  rejection, and feeds dropped during validation \u2014 including a per-rule tally of what was\n  dropped.\n- Removes the YouTube trailer resolver. Clients now hand YouTube trailers to the YouTube\n  app instead, so the server no longer downloads, verifies, updates, or executes yt-dlp or a\n  JavaScript runtime.\n- Removes the yt-dlp and JavaScript-runtime downloads earlier versions installed. Those\n  binaries are unreachable now the resolver is gone, so the plugin reclaims the directory it\n  created rather than leaving an administrator to find it. Only its own install root is\n  touched, and a failure to delete never stops the plugin starting.\n- Fix: Podcast Discovery always returned 404 when enabled. Podcast Index sends the envelope's\n  `status` as a JSON boolean, but it was bound to a string, so the whole recent-feeds payload\n  failed to deserialize. Both the boolean and the documented string form are now accepted, and\n  an unusable payload is logged with the value exactly as it arrived.\n",
                "targetAbi": "10.11.0.0",
                "sourceUrl": "https://github.com/drewtenn/Aurelia-Jellyfin-plugin-dist/releases/download/v1.5.2.0/aurelia-companion_1.5.2.0.zip",
                "checksum": "2dff6cad2883cc25fa4ebaf5a991666c",
                "timestamp": "2026-08-02T04:05:43Z"
            },
            {
                "version": "1.5.1.0",
                "changelog": "- Initial release. - Resolution (isHd/is4K) passthrough for client filtering. - Fix: filtering by a library returned no items. The library (parentId) is now\n  resolved into its contents so video range / codec / audio filters work when\n  scoped to a specific library.\n- Renamed the plugin to \"Aurelia Companion\" (display name only; API route,\n  plugin id, and behavior unchanged).\n- Capabilities are now scoped to the requested library (parentId): the available\n  values offered for a library are drawn only from items in that library, so any\n  single facet selection is guaranteed to match at least one item there. Omitting\n  parentId still returns the global value set.\n- TV libraries now filter correctly by stream facets. Stream attributes live on\n  episodes, but TV libraries are browsed at the series level; the filter now\n  matches episodes and returns the shows that have at least one matching episode.\n  Also forces a recursive query so a library id resolves to its contents.\n- Added opt-in recent podcast discovery through Podcast Index. - Podcast discovery now logs why it returned nothing. The endpoint still answers 404 for\n  every failure, but the server log distinguishes disabled credentials, a Podcast Index\n  rejection, and feeds dropped during validation \u2014 including a per-rule tally of what was\n  dropped.\n- Removes the YouTube trailer resolver. Clients now hand YouTube trailers to the YouTube\n  app instead, so the server no longer downloads, verifies, updates, or executes yt-dlp or a\n  JavaScript runtime.\n- Removes the yt-dlp and JavaScript-runtime downloads earlier versions installed. Those\n  binaries are unreachable now the resolver is gone, so the plugin reclaims the directory it\n  created rather than leaving an administrator to find it. Only its own install root is\n  touched, and a failure to delete never stops the plugin starting.\n",
                "targetAbi": "10.11.0.0",
                "sourceUrl": "https://github.com/drewtenn/Aurelia-Jellyfin-plugin-dist/releases/download/v1.5.1.0/aurelia-companion_1.5.1.0.zip",
                "checksum": "782864137949ce5ce851802d2e50b8ce",
                "timestamp": "2026-08-02T03:32:35Z"
            },
            {
                "version": "1.5.0.0",
                "changelog": "- Initial release. - Resolution (isHd/is4K) passthrough for client filtering. - Fix: filtering by a library returned no items. The library (parentId) is now\n  resolved into its contents so video range / codec / audio filters work when\n  scoped to a specific library.\n- Renamed the plugin to \"Aurelia Companion\" (display name only; API route,\n  plugin id, and behavior unchanged).\n- Capabilities are now scoped to the requested library (parentId): the available\n  values offered for a library are drawn only from items in that library, so any\n  single facet selection is guaranteed to match at least one item there. Omitting\n  parentId still returns the global value set.\n- TV libraries now filter correctly by stream facets. Stream attributes live on\n  episodes, but TV libraries are browsed at the series level; the filter now\n  matches episodes and returns the shows that have at least one matching episode.\n  Also forces a recursive query so a library id resolves to its contents.\n- Added opt-in recent podcast discovery through Podcast Index. - Podcast discovery now logs why it returned nothing. The endpoint still answers 404 for\n  every failure, but the server log distinguishes disabled credentials, a Podcast Index\n  rejection, and feeds dropped during validation \u2014 including a per-rule tally of what was\n  dropped.\n- Removes the YouTube trailer resolver. Clients now hand YouTube trailers to the YouTube\n  app instead, so the server no longer downloads, verifies, updates, or executes yt-dlp or a\n  JavaScript runtime. Delete `<jellyfin-data>/aurelia-trailer-resolver/` to reclaim its disk.\n",
                "targetAbi": "10.11.0.0",
                "sourceUrl": "https://github.com/drewtenn/Aurelia-Jellyfin-plugin-dist/releases/download/v1.5.0.0/aurelia-companion_1.5.0.0.zip",
                "checksum": "5333f540d122429297232358d3da3f97",
                "timestamp": "2026-08-02T03:29:22Z"
            },
            {
                "version": "1.4.0.0",
                "changelog": "- Initial release. - Resolution (isHd/is4K) passthrough for client filtering. - Fix: filtering by a library returned no items. The library (parentId) is now\n  resolved into its contents so video range / codec / audio filters work when\n  scoped to a specific library.\n- Renamed the plugin to \"Aurelia Companion\" (display name only; API route,\n  plugin id, and behavior unchanged).\n- Capabilities are now scoped to the requested library (parentId): the available\n  values offered for a library are drawn only from items in that library, so any\n  single facet selection is guaranteed to match at least one item there. Omitting\n  parentId still returns the global value set.\n- TV libraries now filter correctly by stream facets. Stream attributes live on\n  episodes, but TV libraries are browsed at the series level; the filter now\n  matches episodes and returns the shows that have at least one matching episode.\n  Also forces a recursive query so a library id resolves to its contents.\n- Added opt-in recent podcast discovery through Podcast Index. - Adds an opt-in, authenticated YouTube trailer resolver API. It accepts only canonical\n  video IDs, invokes an administrator-installed executable with fixed arguments, and returns\n  only a short-lived direct HTTPS media URL, safe headers, and its expiry. The executable is\n  not packaged with the plugin.\n- Podcast discovery now logs why it returned nothing. The endpoint still answers 404 for\n  every failure, but the server log distinguishes disabled credentials, a Podcast Index\n  rejection, and feeds dropped during validation \u2014 including a per-rule tally of what was\n  dropped.\n- Adds an optional plugin-managed resolver binary. An administrator selects a source \u2014 an\n  exact pinned release, or the newest stable or nightly build \u2014 and the plugin fetches only\n  official yt-dlp GitHub release assets, verifying each download against that release's\n  published SHA-256 manifest before it is made executable. Both endpoints require elevation,\n  and binaries are stored outside the plugin's version-stamped directory so they survive\n  plugin updates. A failed process start no longer disables the resolver until the server\n  restarts when the executable behind it has changed.\n- A managed resolver now also installs the JavaScript runtime yt-dlp needs (Deno), verified\n  against its published SHA-256 checksum and extracted from the release archive, and points\n  yt-dlp at it. Without a runtime yt-dlp reports YouTube extraction as deprecated and some\n  formats may be missing. It is fetched only when absent, and there is nothing to configure.\n  Hosts with no published runtime \u2014 musl/Alpine \u2014 are reported plainly rather than silently\n  degraded. The resolve timeout moves from 10 to 30 seconds to accommodate runtime start-up\n  and signature deciphering; a timeout would otherwise surface as a silently missing trailer.\n- Adds an optional daily scheduled task that installs the newest build on the tracked\n  channel. It is visible and controllable under Dashboard \u2192 Scheduled Tasks, does nothing\n  unless a tracking source has automatic updates enabled, and never advances a pinned\n  version. The newest three builds per channel are retained so a bad build can be rolled\n  back by pinning the previous one, without a download.\n",
                "targetAbi": "10.11.0.0",
                "sourceUrl": "https://github.com/drewtenn/Aurelia-Jellyfin-plugin-dist/releases/download/v1.4.0.0/aurelia-companion_1.4.0.0.zip",
                "checksum": "1c344ffe2a7cf1e79822c0b387bcf562",
                "timestamp": "2026-08-02T02:16:08Z"
            },
            {
                "version": "1.3.0.0",
                "changelog": "- Initial release. - Resolution (isHd/is4K) passthrough for client filtering. - Fix: filtering by a library returned no items. The library (parentId) is now\n  resolved into its contents so video range / codec / audio filters work when\n  scoped to a specific library.\n- Renamed the plugin to \"Aurelia Companion\" (display name only; API route,\n  plugin id, and behavior unchanged).\n- Capabilities are now scoped to the requested library (parentId): the available\n  values offered for a library are drawn only from items in that library, so any\n  single facet selection is guaranteed to match at least one item there. Omitting\n  parentId still returns the global value set.\n- TV libraries now filter correctly by stream facets. Stream attributes live on\n  episodes, but TV libraries are browsed at the series level; the filter now\n  matches episodes and returns the shows that have at least one matching episode.\n  Also forces a recursive query so a library id resolves to its contents.\n- Added opt-in recent podcast discovery through Podcast Index. - Adds an opt-in, authenticated YouTube trailer resolver API. It accepts only canonical\n  video IDs, invokes an administrator-installed executable with fixed arguments, and returns\n  only a short-lived direct HTTPS media URL, safe headers, and its expiry. The executable is\n  not packaged with the plugin.\n- Podcast discovery now logs why it returned nothing. The endpoint still answers 404 for\n  every failure, but the server log distinguishes disabled credentials, a Podcast Index\n  rejection, and feeds dropped during validation \u2014 including a per-rule tally of what was\n  dropped.\n- Adds an optional plugin-managed resolver binary. An administrator selects a source \u2014 an\n  exact pinned release, or the newest stable or nightly build \u2014 and the plugin fetches only\n  official yt-dlp GitHub release assets, verifying each download against that release's\n  published SHA-256 manifest before it is made executable. Both endpoints require elevation,\n  and binaries are stored outside the plugin's version-stamped directory so they survive\n  plugin updates. A failed process start no longer disables the resolver until the server\n  restarts when the executable behind it has changed.\n- Adds an optional daily scheduled task that installs the newest build on the tracked\n  channel. It is visible and controllable under Dashboard \u2192 Scheduled Tasks, does nothing\n  unless a tracking source has automatic updates enabled, and never advances a pinned\n  version. The newest three builds per channel are retained so a bad build can be rolled\n  back by pinning the previous one, without a download.\n",
                "targetAbi": "10.11.0.0",
                "sourceUrl": "https://github.com/drewtenn/Aurelia-Jellyfin-plugin-dist/releases/download/v1.3.0.0/aurelia-companion_1.3.0.0.zip",
                "checksum": "494101e45375a6b3aed57c6c8e23b707",
                "timestamp": "2026-08-02T01:43:36Z"
            },
            {
                "version": "1.2.0.1",
                "changelog": "- Initial release. - Resolution (isHd/is4K) passthrough for client filtering. - Fix: filtering by a library returned no items. The library (parentId) is now\n  resolved into its contents so video range / codec / audio filters work when\n  scoped to a specific library.\n- Renamed the plugin to \"Aurelia Companion\" (display name only; API route,\n  plugin id, and behavior unchanged).\n- Capabilities are now scoped to the requested library (parentId): the available\n  values offered for a library are drawn only from items in that library, so any\n  single facet selection is guaranteed to match at least one item there. Omitting\n  parentId still returns the global value set.\n- TV libraries now filter correctly by stream facets. Stream attributes live on\n  episodes, but TV libraries are browsed at the series level; the filter now\n  matches episodes and returns the shows that have at least one matching episode.\n  Also forces a recursive query so a library id resolves to its contents.\n- Added opt-in recent podcast discovery through Podcast Index. - Adds an opt-in, authenticated YouTube trailer resolver API. It accepts only canonical\n  video IDs, invokes an administrator-installed executable with fixed arguments, and returns\n  only a short-lived direct HTTPS media URL, safe headers, and its expiry. The executable is\n  not packaged with the plugin.\n",
                "targetAbi": "10.11.0.0",
                "sourceUrl": "https://github.com/drewtenn/Aurelia-Jellyfin-plugin-dist/releases/download/v1.2.0.1/aurelia-companion_1.2.0.1.zip",
                "checksum": "61947264068fbed2e7317934c298ee1e",
                "timestamp": "2026-08-01T22:26:48Z"
            },
            {
                "version": "1.2.0.0",
                "changelog": "- Initial release. - Resolution (isHd/is4K) passthrough for client filtering. - Fix: filtering by a library returned no items. The library (parentId) is now\n  resolved into its contents so video range / codec / audio filters work when\n  scoped to a specific library.\n- Renamed the plugin to \"Aurelia Companion\" (display name only; API route,\n  plugin id, and behavior unchanged).\n- Capabilities are now scoped to the requested library (parentId): the available\n  values offered for a library are drawn only from items in that library, so any\n  single facet selection is guaranteed to match at least one item there. Omitting\n  parentId still returns the global value set.\n- TV libraries now filter correctly by stream facets. Stream attributes live on\n  episodes, but TV libraries are browsed at the series level; the filter now\n  matches episodes and returns the shows that have at least one matching episode.\n  Also forces a recursive query so a library id resolves to its contents.\n- Added opt-in recent podcast discovery through Podcast Index.\n",
                "targetAbi": "10.11.0.0",
                "sourceUrl": "https://github.com/drewtenn/Aurelia-Jellyfin-plugin-dist/releases/download/v1.2.0.0/aurelia-companion_1.2.0.0.zip",
                "checksum": "6cd2c0602c64eccca0d3a74839a2ecd9",
                "timestamp": "2026-08-01T18:36:00Z"
            },
            {
                "version": "1.1.4.0",
                "changelog": "- Initial release. - Resolution (isHd/is4K) passthrough for client filtering. - Fix: filtering by a library returned no items. The library (parentId) is now\n  resolved into its contents so video range / codec / audio filters work when\n  scoped to a specific library.\n- Renamed the plugin to \"Aurelia Companion\" (display name only; API route,\n  plugin id, and behavior unchanged).\n- Capabilities are now scoped to the requested library (parentId): the available\n  values offered for a library are drawn only from items in that library, so any\n  single facet selection is guaranteed to match at least one item there. Omitting\n  parentId still returns the global value set.\n- TV libraries now filter correctly by stream facets. Stream attributes live on\n  episodes, but TV libraries are browsed at the series level; the filter now\n  matches episodes and returns the shows that have at least one matching episode.\n  Also forces a recursive query so a library id resolves to its contents.\n",
                "targetAbi": "10.11.0.0",
                "sourceUrl": "https://github.com/drewtenn/Aurelia-Jellyfin-plugin-dist/releases/download/v1.1.4.0/aurelia-companion_1.1.4.0.zip",
                "checksum": "907dba752839f6cc96a486eb6b241c24",
                "timestamp": "2026-06-16T01:35:38Z"
            },
            {
                "version": "1.1.3.0",
                "changelog": "- Initial release. - Resolution (isHd/is4K) passthrough for client filtering. - Fix: filtering by a library returned no items. The library (parentId) is now\n  resolved into its contents so video range / codec / audio filters work when\n  scoped to a specific library.\n- Renamed the plugin to \"Aurelia Companion\" (display name only; API route,\n  plugin id, and behavior unchanged).\n- Capabilities are now scoped to the requested library (parentId): the available\n  values offered for a library are drawn only from items in that library, so any\n  single facet selection is guaranteed to match at least one item there. Omitting\n  parentId still returns the global value set.\n",
                "targetAbi": "10.11.0.0",
                "sourceUrl": "https://github.com/drewtenn/Aurelia-Jellyfin-plugin-dist/releases/download/v1.1.3.0/aurelia-companion_1.1.3.0.zip",
                "checksum": "3ef24efaf0946efd08be11214b877dc1",
                "timestamp": "2026-06-16T01:03:59Z"
            },
            {
                "version": "1.1.2.0",
                "changelog": "- Initial release. - Resolution (isHd/is4K) passthrough for client filtering. - Fix: filtering by a library returned no items. The library (parentId) is now\n  resolved into its contents so video range / codec / audio filters work when\n  scoped to a specific library.\n- Renamed the plugin to \"Aurelia Companion\" (display name only; API route,\n  plugin id, and behavior unchanged).\n",
                "targetAbi": "10.11.0.0",
                "sourceUrl": "https://github.com/drewtenn/Aurelia-Jellyfin-plugin-dist/releases/download/v1.1.2.0/aurelia-companion_1.1.2.0.zip",
                "checksum": "a3f4eb90a3a53207e12ffef4110d412c",
                "timestamp": "2026-06-16T00:03:27Z"
            },
            {
                "version": "1.1.1.0",
                "changelog": "- Initial release. - Resolution (isHd/is4K) passthrough for client filtering. - Fix: filtering by a library returned no items. The library (parentId) is now\n  resolved into its contents so video range / codec / audio filters work when\n  scoped to a specific library.\n",
                "targetAbi": "10.11.0.0",
                "sourceUrl": "https://github.com/drewtenn/Aurelia-Jellyfin-plugin-dist/releases/download/v1.1.1.0/media-stream-filters_1.1.1.0.zip",
                "checksum": "a1940db1576c777bb3bc93d07775fa9a",
                "timestamp": "2026-06-15T22:47:31Z"
            },
            {
                "version": "1.1.0.0",
                "changelog": "- Initial release. - Resolution (isHd/is4K) passthrough for client filtering.\n",
                "targetAbi": "10.11.0.0",
                "sourceUrl": "https://github.com/drewtenn/Aurelia-Jellyfin-plugin-dist/releases/download/v1.1.0.0/media-stream-filters_1.1.0.0.zip",
                "checksum": "c67629c04bf0bedef7544795e87fa718",
                "timestamp": "2026-06-15T20:08:46Z"
            }
        ]
    }
]